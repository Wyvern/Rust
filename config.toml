[alias]
rr = "run --release"
br = "build --release"

[cargo-new]
vcs = "none" 

[profile.release]
rpath = true
#strip="symbols"
#lto=true

[profile.dev]
rpath = true

[build]
rustflags = [
"-C","target-feature=-crt-static",
"-C","target-cpu=native",
#"-C","panic=abort",
"-C","prefer-dynamic"
]

# $ORIGIN/../lib | @loader_path/../lib
# "-C","link-args=-Wl,-rpath,@loader_path/../lib",
